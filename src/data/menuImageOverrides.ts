// Verified replacement images live here so the original menuImage mapping/assets
// can stay untouched until the replacements are reviewed and approved.
export const menuImageOverrides: Record<string, string> = {
};
